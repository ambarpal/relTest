<h1>Listing bs</h1>

<table>
  <thead>
    <tr>
      <th>Surname</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bs.each do |b| %>
      <tr>
        <td><%= b.surname %></td>
        <td><%= link_to 'Show', b %></td>
        <td><%= link_to 'Edit', edit_b_path(b) %></td>
        <td><%= link_to 'Destroy', b, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New B', new_b_path %>
