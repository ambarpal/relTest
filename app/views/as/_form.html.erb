<%= form_for(@a) do |f| %>
<% if @a.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@a.errors.count, "error") %> prohibited this a from being saved:</h2>

  <ul>
    <% @a.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <%= f.label :bs %><br>
  <%= select_tag 'surname', options_from_collection_for_select(B.all, 'id' , 'surname'), :multiple => true %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
